// Hero banner should always fill the viewport while keeping the full image visible.
export const HERO_HEIGHT_CLASSES = 'h-screen min-h-[100svh]';
export const NAV_WIDTH_CLASSES = 'mx-auto w-full max-w-7xl px-4 sm:px-6 lg:px-8';