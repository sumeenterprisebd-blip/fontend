// Hero banner: use a consistent wide aspect ratio so the full image can be shown
// without top/bottom letterboxing (works with object-contain) across all viewports.
export const HERO_HEIGHT_CLASSES = 'h-[23vh] min-h-[150px] md:h-[48vh] lg:h-[50vh] xl:h-[55vh]';
export const NAV_WIDTH_CLASSES = 'max-w-7xl mx-auto px-4 sm:px-6 lg:px-8';